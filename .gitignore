# Store .md files and documentation only; this is deliberate
# Ignore everything by default
*

# Allow .gitignore itself
!.gitignore

# Allow all .md files in root directory only
!/*.md

# Allow everything in doc directory
!doc/
!doc/**

# Allow dist directory and all binaries in it
!dist/
!dist/**